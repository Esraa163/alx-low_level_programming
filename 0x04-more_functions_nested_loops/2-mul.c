#include "main.h"

/**
 * mul - that multibly two numbers
 *
 * @a: first input
 *
 * @b: second input
 *
 * Return: always (a * b) where a and b is two numbers
*/

int mul(int a, int b)
{
	return (a * b);
}
